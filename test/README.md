Test yig function and performance

# Require
+ pip (python2)

# Install python pkgs
```
    sudo pip install --ignore-installed boto3
    sudo pip install --ignore-installed matplotlib
```

# Run
```
    sudo python sanity.py
```